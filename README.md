### Steps:

1. Clone repo
2. In IntelliJ:
   1. Right Click project root folder (Stop-Motion-App) and select Open In --> Terminal
   2. Run the command "npm install" (installs all dependencies for the server)
   3. Run the command "npm run dev" (starts the Vite dev server and opens the browser)
